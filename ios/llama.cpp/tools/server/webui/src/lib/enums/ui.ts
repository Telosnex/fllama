export enum ColorMode {
	LIGHT = 'light',
	DARK = 'dark',
	SYSTEM = 'system'
}

/**
 * URL prefixes for protocol detection
 */
export enum UrlPrefix {
	DATA = 'data:',
	HTTP = 'http://',
	HTTPS = 'https://',
	WEBSOCKET = 'ws://',
	WEBSOCKET_SECURE = 'wss://'
}
